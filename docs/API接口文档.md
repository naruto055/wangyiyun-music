# 网易云音乐 API 接口文档

> 基于 Spring Boot 的网易云音乐后端服务系统 API 文档
>
> **文档版本**: v1.0.0
> **生成时间**: 2026-01-26 00:04:51
> **基础路径**: http://localhost:8910

---

## 📋 目录

- [基础信息](#基础信息)
- [通用响应结构](#通用响应结构)
- [接口列表](#接口列表)
  - [音乐管理](#1-音乐管理)
  - [收藏管理](#2-收藏管理)
  - [播放记录管理](#3-播放记录管理)
- [数据模型](#数据模型)

---

## 基础信息

### 服务地址

- **开发环境**: http://localhost:8910
- **API 文档（Swagger UI）**: http://localhost:8910/swagger-ui/index.html
- **OpenAPI JSON**: http://localhost:8910/v3/api-docs

### 请求说明

- **Content-Type**: application/json
- **字符编码**: UTF-8
- **请求方法**: GET、POST、PUT、DELETE

### 响应说明

所有接口返回统一格式，详见 [通用响应结构](#通用响应结构)

---

## 通用响应结构

### Result<T> 统一响应对象

| 字段 | 类型 | 描述 | 示例 |
|------|------|------|------|
| code | Integer | 响应码，200 表示成功 | 200 |
| message | String | 响应消息 | "操作成功" |
| data | T | 响应数据，具体类型视接口而定 | {...} |

**成功响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "name": "七里香"
  }
}
```

**失败响应示例**:

```json
{
  "code": 500,
  "message": "系统错误",
  "data": null
}
```

---

## 接口列表

### 1. 音乐管理

**模块路径**: `/api/music`

#### 1.1 分页查询音乐列表

- **接口描述**: 支持按分类、标签、关键词等���件筛选音乐，返回包含歌手名称的音乐列表
- **请求方法**: `GET`
- **请求路径**: `/api/music/list`
- **请求参数**:

| 参数名 | 类型 | 必填 | 默认值 | 描述 | 示例 |
|--------|------|------|--------|------|------|
| pageNum | Integer | 否 | 1 | 页码 | 1 |
| pageSize | Integer | 否 | 10 | 每页大小 | 10 |
| categoryId | Long | 否 | - | 分类ID | 1 |
| keyword | String | 否 | - | 关键词（歌曲名/歌手名） | "周杰伦" |
| sortField | String | 否 | create_time | 排序字段 | "play_count" |
| sortOrder | String | 否 | desc | 排序方式（asc/desc） | "desc" |

- **响应数据**: `Result<IPage<MusicListVO>>`
  - `data.records`: 音乐列表
  - `data.total`: 总记录数
  - `data.pages`: 总页数
  - `data.current`: 当前页码
  - `data.size`: 每页大小

**请求示例**:

```bash
GET /api/music/list?pageNum=1&pageSize=10&keyword=周杰伦&sortField=play_count&sortOrder=desc
```

**响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [
      {
        "id": 1,
        "title": "七里香",
        "fileUrl": "https://music.example.com/1.mp3",
        "coverUrl": "https://cover.example.com/1.jpg",
        "duration": 299,
        "playCount": 1000000,
        "artistNames": "周杰伦",
        "categoryId": 1,
        "updateTime": "2026-01-01 10:00:00",
        "createTime": "2026-01-01 10:00:00"
      }
    ],
    "total": 100,
    "pages": 10,
    "current": 1,
    "size": 10
  }
}
```

> **说明**：
> - `artistNames`: 歌手名称，多个歌手用 "/" 分隔，例如："周杰伦/方文山"
> - 日期时间格式统一为：`yyyy-MM-dd HH:mm:ss`

---

#### 1.2 获取音乐详情

- **接口描述**: 根据音乐ID查询详细信息，包含歌手、专辑、标签等关联数据
- **请求方法**: `GET`
- **请求路径**: `/api/music/{id}`
- **请求参数**:

| 参数名 | 类型 | 必填 | 描述 | 示例 |
|--------|------|------|------|------|
| id | Long | 是 | 音乐ID | 1 |

- **响应数据**: `Result<MusicDetailVO>`

**请求示例**:

```bash
GET /api/music/1
```

**响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "name": "七里香",
    "url": "https://music.example.com/1.mp3",
    "coverUrl": "https://cover.example.com/1.jpg",
    "duration": 299,
    "playCount": 1000000,
    "categoryId": 1,
    "artists": [
      {
        "id": 1,
        "name": "周杰伦",
        "avatarUrl": "https://artist.example.com/jay.jpg"
      }
    ],
    "tags": [
      {
        "id": 1,
        "name": "流行"
      }
    ]
  }
}
```

---

### 2. 收藏管理

**模块路径**: `/api/favorite`

#### 2.1 收藏音乐

- **接口描述**: 将指定音乐添加到用户的收藏列表
- **请求方法**: `POST`
- **请求路径**: `/api/favorite/{musicId}`
- **请求参数**:

| 参数名 | 类型 | 必填 | 描述 | 示例 |
|--------|------|------|------|------|
| musicId | Long | 是 | 音乐ID | 1 |

- **响应数据**: `Result<Void>`

**请求示例**:

```bash
POST /api/favorite/1
```

**响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": null
}
```

---

#### 2.2 取消收藏

- **接口描述**: 从用户的收藏列表中移除指定音乐
- **请求方法**: `DELETE`
- **请求路径**: `/api/favorite/{musicId}`
- **请求参数**:

| 参数名 | 类型 | 必填 | 描述 | 示例 |
|--------|------|------|------|------|
| musicId | Long | 是 | 音乐ID | 1 |

- **响应数据**: `Result<Void>`

**请求示例**:

```bash
DELETE /api/favorite/1
```

**响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": null
}
```

---

#### 2.3 查询收藏列表

- **接口描述**: 分页查询用户的音乐收藏列表
- **请求方法**: `GET`
- **请求路径**: `/api/favorite/list`
- **请求参数**:

| 参数名 | 类型 | 必填 | 默认值 | 描述 | 示例 |
|--------|------|------|--------|------|------|
| pageNum | Integer | 否 | 1 | 页码 | 1 |
| pageSize | Integer | 否 | 10 | 每页大小 | 10 |

- **响应数据**: `Result<IPage<Favorite>>`

**请求示例**:

```bash
GET /api/favorite/list?pageNum=1&pageSize=10
```

**响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [
      {
        "id": 1,
        "userId": 1,
        "musicId": 1,
        "createTime": "2026-01-01 10:00:00"
      }
    ],
    "total": 50,
    "pages": 5,
    "current": 1,
    "size": 10
  }
}
```

---

### 3. 播放记录管理

**模块路径**: `/api/play-record`

#### 3.1 记录播放

- **接口描述**: 记录用户播放音乐的详细信息，包括播放进度、播放时长等
- **请求方法**: `POST`
- **请求路径**: `/api/play-record`
- **请求参数** (Request Body):

| 字段 | 类型 | 必填 | 默认值 | 描述 | 示例 |
|------|------|------|--------|------|------|
| musicId | Long | 是 | - | 音乐ID | 1 |
| playDuration | Integer | 否 | - | 播放时长（秒） | 180 |
| playSource | String | 否 | web | 播放来源 | "web" |

**请求示例**:

```bash
POST /api/play-record
Content-Type: application/json

{
  "musicId": 1,
  "playDuration": 180,
  "playSource": "web"
}
```

**响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": null
}
```

---

#### 3.2 查询播放历史

- **接口描述**: 分页查询用户的播放历史记录
- **请求方法**: `GET`
- **请求路径**: `/api/play-record/history`
- **请求参数**:

| 参数名 | 类型 | 必填 | 默认值 | 描述 | 示例 |
|--------|------|------|--------|------|------|
| pageNum | Integer | 否 | 1 | 页码 | 1 |
| pageSize | Integer | 否 | 10 | 每页大小 | 10 |

- **响应数据**: `Result<IPage<PlayRecord>>`

**请求示例**:

```bash
GET /api/play-record/history?pageNum=1&pageSize=10
```

**响应示例**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [
      {
        "id": 1,
        "userId": 1,
        "musicId": 1,
        "playDuration": 180,
        "playSource": "web",
        "createTime": "2026-01-01 10:00:00"
      }
    ],
    "total": 200,
    "pages": 20,
    "current": 1,
    "size": 10
  }
}
```

---

## 数据模型

### Music (音乐实体)

| 字段 | 类型 | 描述 | 示例 |
|------|------|------|------|
| id | Long | 音乐ID | 1 |
| name | String | 音乐名称 | "七里香" |
| url | String | 音乐文件URL | "https://music.example.com/1.mp3" |
| coverUrl | String | 封面图片URL | "https://cover.example.com/1.jpg" |
| duration | Integer | 时长（秒） | 299 |
| playCount | Integer | 播放量 | 1000000 |
| categoryId | Long | 分类ID | 1 |
| createTime | DateTime | 创建时间 | "2026-01-01 10:00:00" |

### MusicDetailVO (音乐详情)

包含 Music 的所有字段，以及：

| 字段 | 类型 | 描述 | 示例 |
|------|------|------|------|
| artists | List&lt;Artist&gt; | 歌手列表 | [...] |
| tags | List&lt;Tag&gt; | 标签列表 | [...] |

### Favorite (收藏)

| 字段 | 类型 | 描述 | 示例 |
|------|------|------|------|
| id | Long | 收藏ID | 1 |
| userId | Long | 用户ID | 1 |
| musicId | Long | 音乐ID | 1 |
| createTime | DateTime | 收藏时间 | "2026-01-01 10:00:00" |

### PlayRecord (播放记录)

| 字段 | 类型 | 描述 | 示例 |
|------|------|------|------|
| id | Long | 记录ID | 1 |
| userId | Long | 用户ID | 1 |
| musicId | Long | 音乐ID | 1 |
| playDuration | Integer | 播放时长（秒） | 180 |
| playSource | String | 播放来源 | "web" |
| createTime | DateTime | 创建时间 | "2026-01-01 10:00:00" |

---

## 实现说明

### 统一响应封装机制

本项目采用 `@RestControllerAdvice` + `ResponseBodyAdvice` 实现统一响应封装，确保所有 API 接口返回格式一致。

#### 工作原理

**核心组件**：[GlobalResponseAdvice](src/main/java/com/naruto/wangyiyunmusic/config/GlobalResponseAdvice.java)

```
Controller 方法返回值 → GlobalResponseAdvice 自动包装 → HTTP 响应 Result<T>
```

**封装规则**：

1. **自动封装**：所有 Controller 方法返回值自动包装为 `Result<T>`
2. **类型判断**：
   - 如果返回值已经是 `Result` 类型，直接返回
   - 如果返回值为 `null`，返回空 `Result`
   - 如果返回值是 `String` 类型，特殊处理后返回（避免类型转换问题）
   - 其他类型统一封装为 `Result.success(body)`

3. **排除机制**：支持通过 `[@IgnoreResponseWrap](src/main/java/com/naruto/wangyiyunmusic/annotation/IgnoreResponseWrap.java)` 注解排除特定接口
   - 可标注在方法上：排除单个接口
   - 可标注在类上：排除整个 Controller

#### 代码示例

**重构前**（手动包装）：
```java
@GetMapping("/list")
public Result<IPage<Music>> list(MusicQueryDTO queryDTO) {
    IPage<Music> page = musicService.pageQuery(queryDTO);
    return Result.success(page);  // 手动包装
}
```

**重构后**（自动包装）：
```java
@GetMapping("/list")
public IPage<Music> list(MusicQueryDTO queryDTO) {
    return musicService.pageQuery(queryDTO);  // 直接返回业务对象
}
```

**排除自动封装的示例**：
```java
@IgnoreResponseWrap  // 排除此接口的自动封装
@GetMapping("/export")
public void exportData(HttpServletResponse response) {
    // 文件下载等特殊场景
}
```

#### 设计优势

- ✅ **简化代码**：减少 Controller 中的重复 `Result.success()` 调用
- ✅ **统一规范**：确保所有接口响应格式一致
- ✅ **灵活可控**：支持排除机制，满足特殊场景需求
- ✅ **零侵入性**：对前端调用者透明，无需修改调用代码

#### 注意事项

- 异常处理由 [GlobalExceptionHandler](src/main/java/com/naruto/wangyiyunmusic/exception/GlobalExceptionHandler.java) 统一处理，与响应封装职责分离
- 文档中记录的 **HTTP 响应格式** 均为 `Result<T>`，不受实现细节影响

---

## 附录

### A. 状态码说明

| 状态码 | 描述 |
|--------|------|
| 200 | 请求成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### B. 分页说明

所有分页接口统一使用 MyBatis-Plus 的 `IPage` 对象：

| 字段 | 类型 | 描述 |
|------|------|------|
| records | List&lt;T&gt; | 当前页数据列表 |
| total | Long | 总记录数 |
| pages | Long | 总页数 |
| current | Long | 当前页码 |
| size | Long | 每页大小 |

### C. 相关链接

- [Swagger UI 在线文档](http://localhost:8910/swagger-ui/index.html)
- [项目 GitHub 仓库](#) (待添加)

---

**文档更新日志**:

- **2026-01-26**:
  - 初始版本，包含 7 个基础接口文档
  - 新增"实现说明"章节，说明统一响应封装机制
  - 更新音乐列表接口，返回 MusicListVO，添加 artistNames 字段

---

**维护者**: naruto
**许可证**: Apache 2.0
