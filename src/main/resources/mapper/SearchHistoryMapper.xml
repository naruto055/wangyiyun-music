<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.naruto.wangyiyunmusic.mapper.SearchHistoryMapper">

    <!-- 根据用户ID和关键词查询历史记录 -->
    <select id="selectByUserAndKeyword" resultType="com.naruto.wangyiyunmusic.model.entity.SearchHistory">
        SELECT
            id,
            user_id,
            keyword,
            search_count,
            last_search_time,
            create_time,
            update_time
        FROM search_history
        WHERE keyword = #{keyword}
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="userId == null">
            AND user_id IS NULL
        </if>
        LIMIT 1
    </select>

    <!-- 增加搜索次数 -->
    <update id="incrementSearchCount">
        UPDATE search_history
        SET search_count = search_count + 1,
            last_search_time = NOW(),
            update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>
